import React from "react";

function Button() {
    return (
        <div>
                <button type="submit"></button>
        </div>
    );
}
//need to do event for submit button

export default Button;